### Basic usage instructions

1. setup imports using `pip install -r requirements.txt`
2. have best_micpl_model.pth.zip in the `app/models/`
3. launch backend using `uvicorn app.main:app --reload` (note: be in `backend/` folder when doing this)
4. launch index.html
